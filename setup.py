# This file is required for compatibility with older versions of pip/setuptools that don't support pyproject.toml configuration.

from setuptools import setup

if __name__ == "__main__":
    setup()
